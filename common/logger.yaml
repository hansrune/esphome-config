---
# Logger component
#
# The logger component automatically logs all log messages through the serial
# port and through MQTT topics.
#
# https://esphome.io/components/logger.html
#
logger:
  id: ${system_id}_logger
  # Set to 0 to disable
  baud_rate: 115200
  # level: VERBOSE
  #hardware_uart: USB_SERIAL_JTAG
  #hardware_uart: uart0
