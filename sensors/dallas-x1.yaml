---
#
# Generix 1 wire dallas sensor when just one is being used
#
platform: dallas_temp
name: "${friendly_name} 1-wire sensor"
id: ${system_id}_1wire_temperature
one_wire_id: wire_5
#index: 0 # only one sensor per wire - so index is safe
